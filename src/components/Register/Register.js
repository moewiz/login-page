import React from 'react';

import {RegisterWrapper} from "./styled";

const Register = () => {
  return (
    <RegisterWrapper>
      Register
    </RegisterWrapper>
  );
};

export default Register;
