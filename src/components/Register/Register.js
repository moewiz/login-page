import React from 'react';

import {LoginLayoutWrapper} from "../Layouts/styled";

const Register = () => {
  return (
    <LoginLayoutWrapper>
      Register
    </LoginLayoutWrapper>
  );
};

export default Register;
